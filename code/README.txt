1. Single color (black and white)
2. Batch transform all the images to single color which is a (x,y,n) matrix

In feature extraction, necessary steps are :
    1. [Raw Extraction] extract all raw features from images library
    2. [   Build      ]  build encoding space
    3. [Encode] encode each images in the encoding space